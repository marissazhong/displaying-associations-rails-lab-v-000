<%= link_to @artist.name, artist_path(@artist) %>
<%= @artist.song_count %> songs

<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_path(song) %></li>
  <% end %>
</ul>
